{
  "name": "devpilot-ai-github-integration",
  "version": "1.0.0",
  "description": "GitHub Integration for devpilot-ai supporting OAuth, Repositories, Branches, Commits, PRs, Issues, Webhooks, and Tests.",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/**/*.test.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "octokit": "^4.1.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
